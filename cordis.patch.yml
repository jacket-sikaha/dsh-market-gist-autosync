# dsh bundle patch: mounts this plugin (host half) into the profile layer stack.
- insert:
    - id: dsh-market-gist-autosync
      name: 'dsh-market-gist-autosync'
      config:
        gistApiHost: api.github.com
